flysystem:
    storages:
        default.storage:
            adapter: 'memory'

        file.storage:
            adapter: 'memory'

        database.storage:
            adapter: 'memory'

        build_html.storage:
            adapter: 'memory'