{% autoescape false %}
    <a class="h-104 w-full flex flex-col items-center" href="{{ relativeUrl('activities/' ~ activity.getId()) }}" data-router-link target="_blank">
        <img class="max-h-full max-w-full rounded-lg lazy" data-src="{{ relativeUrl(image.getImageUrl()) }}" alt="{{ activity.getName() }}" src="{{ placeholderImage(image.getOrientation()) }}" />
    </a>
{% endautoescape %}