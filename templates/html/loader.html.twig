<div role="status" class="flex flex-col items-center gap-y-4">
    <div class="{{ ('loader ' ~ modifier|default(''))|trim }}"></div>
    <div class="loader-messages">
        <span>{{ "Crunching the numbers..."|trans }}</span>
        <span>{{ "Recalculating your suffer score..."|trans }}</span>
        <span>{{ "Waxing the chain..."|trans }}</span>
        <span>{{ "Dusting off the record books..."|trans }}</span>
        <span>{{ "Filling up the bottles..."|trans }}</span>
    </div>
</div>
