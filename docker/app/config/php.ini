# date.timezone is set in docker-entrypoint based on what the user configured in the .env
expose_php= Off

memory_limit=1024M

opcache.enable=1
opcache.revalidate_freq=10
opcache.validate_timestamps=1
opcache.max_accelerated_files=10000
opcache.memory_consumption=192
opcache.max_wasted_percentage=10
opcache.interned_strings_buffer=1
opcache.fast_shutdown=1

upload_max_filesize = 20M
; Images are base64-encoded into the JSON dispatchCommand body, not uploaded as files,
; so post_max_size is the real ceiling: 30M of images inflates to roughly 40M over the wire.
post_max_size = 48M
max_execution_time = 120
